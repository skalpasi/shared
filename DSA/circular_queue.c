#include <stdio.h>

/* problem with two ptr queue - when we dequeue in two
ptr queue, the front pointer moves forward and it decreases
the size of the queue. */

struct Queue {

} queue;

int main() {
  queue;
  return 0;
}
